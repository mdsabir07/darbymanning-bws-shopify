{% paginate collection.products by settings.pagination_limit %}
	<section class="o-grid">
		<div class="o-grid__item  u-size-12@s u-mB-20">
		   {% include 'breadcrumbs', paginate: paginate %}
		</div>
	</section>
{% endpaginate %}

<section class="collection-header  o-grid">
    <div class="o-grid__item  u-size-12@s">
        <div class="block  block--row  border">
            <div class="block__col">
                <div class="block__info  block__info--light-gray  block__info--large  collection-header__info">
                    <h1 class="block__title  block__title--large  block__title--p-t  u-textUpper  collection-header__title  nimbus">{{ collection.title }}</h1>
                    {% if collection.description != blank %}
                        <div class="collection-header__content">
                            {{ collection.description }}
                        </div>
                        {% if collection.description.size > 480 %}
                        	<button class="collection-header__more js-see-more">See More</button>
                        {% endif %}
                    {% endif %}
                </div>
            </div>

            <div class="collection-header__img  block__col">
                {% if collection.image != blank %}
                <img src="{{ collection.image | img_url: 'master' }}" alt="{{ collection.image.alt }}">
                {% else %}
                <img src="{{ products.first.featured_image | img_url: 'master' }}" alt="{{ products.first.featured_image.alt }}"/>
                {% endif %}
            </div>
        </div>
    </div>
</section>
